version: 2
jobs:
  build:
    docker:
      - image: yuanz271/texlive
    steps:
      - run: apt-get -y update && apt-get -y install git make curl
      - checkout
      - run: make
      - deploy: 
          command: sh upload.sh